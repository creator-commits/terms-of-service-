name: Build and Deploy

on:
  push:
    branches: [main]
  repository_dispatch:
    types: [contact_form]

jobs:

  # ── Job 1: Send email when form submitted ──
  send-email:
    if: github.event_name == 'repository_dispatch'
    runs-on: ubuntu-latest
    steps:
      - name: Send Email
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: smtp.gmail.com
          server_port: 465
          secure: true
          username: ${{ secrets.MAIL_USERNAME }}
          password: ${{ secrets.MAIL_PASSWORD }}
          to: ${{ secrets.MAIL_TO }}
          from: ${{ secrets.MAIL_USERNAME }}
          subject: "[${{ github.event.client_payload.type }}] ${{ github.event.client_payload.subject }}"
          body: |
            Type:    ${{ github.event.client_payload.type }}
            Name:    ${{ github.event.client_payload.name }}
            From:    ${{ github.event.client_payload.email }}
            Subject: ${{ github.event.client_payload.subject }}
            Request: ${{ github.event.client_payload.reqtype }}
            Work:    ${{ github.event.client_payload.work }}
            Link:    ${{ github.event.client_payload.link }}

            Message:
            ${{ github.event.client_payload.message }}

            ---
            Sent from [@]Creators Terms of Service page

  # ── Job 2: Inject secrets + deploy to gh-pages branch ──
  deploy:
    if: github.event_name == 'push'
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Inject secrets
        run: |
          TOKEN="${{ secrets.GH_PAT }}"
          REPO="${{ github.repository }}"
          sed -i "s|%%GH_TOKEN%%|${TOKEN}|g" index.html
          sed -i "s|%%GH_REPO%%|${REPO}|g" index.html

      - name: Deploy to gh-pages branch
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: .
          publish_branch: gh-pages
